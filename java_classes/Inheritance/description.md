In java it is possible to inherit the properties from parent class to child class. We group the Inheritance concept into two categories:

- Subclass(Child) : The class that inherits properties from another class.
- Superclass(Parent) : The class that being inherited from or the properties of this class is inherited by the subclass.

We can inherit the property of a class by "Extends" Keyword.

